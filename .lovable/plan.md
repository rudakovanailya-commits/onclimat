

## План: Тёмная и светлая тема

### Что будет сделано

1. **Dark-переменные в CSS** — добавить блок `.dark` в `src/index.css` с тёмными значениями всех CSS-переменных (тёмный фон, светлый текст, приглушённые карточки/бордеры, скорректированные тени и градиенты).

2. **Компонент ThemeToggle** — новый `src/components/ThemeToggle.tsx` с иконками Sun/Moon. При клике переключает класс `dark` на `<html>`, сохраняет выбор в `localStorage`, при загрузке читает сохранённое значение (или системные настройки).

3. **Интеграция в Header** — добавить `ThemeToggle` в шапку сайта рядом с кнопками (десктоп и мобильное меню).

4. **Tailwind config** — уже настроен `darkMode: ["class"]`, изменения не нужны.

### Файлы

| Файл | Действие |
|---|---|
| `src/index.css` | Добавить `.dark { ... }` с тёмными переменными |
| `src/components/ThemeToggle.tsx` | Новый компонент |
| `src/components/Header.tsx` | Добавить ThemeToggle |

